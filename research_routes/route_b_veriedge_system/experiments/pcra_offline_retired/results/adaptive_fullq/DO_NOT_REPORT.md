# Diagnostic output only

This run predates the harm-preserving constraint and permits the optimizer to
cancel measured honest residual. Do not report these rates. Use the results in
`../adaptive_harm_preserving/` instead.
